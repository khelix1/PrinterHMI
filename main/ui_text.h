#pragma once

/* English is the source catalog. Future language packs translate these same
 * stable source strings without changing page/controller code. */
const char *ui_text(const char *english_source);
